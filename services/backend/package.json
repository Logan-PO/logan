{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "test": "jest --passWithNoTests",
        "deploy": "sam deploy --stack-name logan-backend"
    },
    "dependencies": {
        "aws-sdk": "^2.437.0"
    },
    "devDependencies": {
        "jest": "^24.7.1"
    }
}

