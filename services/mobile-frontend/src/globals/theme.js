import { DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: 'teal',
        accent: 'orange',
        background: 'white',
    },
};

export default theme;
